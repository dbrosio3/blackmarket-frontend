import { Box, chakra } from '@chakra-ui/react';

export const AuthSection = chakra(Box, {
  baseStyle: {
    bg: 'white',
    width: '22.5rem',
    borderRadius: '8px',
    padding: '48px, 10px, 48px, 10px',
  },
});
